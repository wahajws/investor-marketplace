{
  "installCommand": "npm install",
  "buildCommand": "npm run prisma:generate -w apps/api && if [ -d apps/web ]; then npm --prefix apps/web run build; else npm run build; fi",
  "outputDirectory": "apps/web/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/api/v1/:path*",
      "destination": "/api/v1?__path=:path*"
    },
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
